<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Library</title>
</head>
<body>
<header><h1>Library</h1></header>
<nav>
    <a href="/">Home</a>
    <a href="#">Create</a>
</nav>
<h2>{{book.getTitle()}}</h2>
<ul>
    <li><small>Tác giả: {{book.getAuthor()}}</small></li>
    <li><small>Nhà xuất bản: {{book.getPublisher().getName()}}</small></li>
    <li><small>Giá: {{book.getPrice()}}</small></li>
</ul>
<form action="/edit/{{book.getId()}}" method="get">
    <button>Edit</button>
</form>
<form action="/delete/{{book.getId()}}" method="get">
    <button>Delete</button>
</form>
</body>
</html>